User question:
$question$

User profile:
$userProfile$

Conversation history:
$conversationHistory$

Retrieved RAG context:
$ragContext$

Tool results so far:
$toolHistory$

Request metadata:
$metadata$

Produce either a tool plan or a final answer using the JSON contract.
